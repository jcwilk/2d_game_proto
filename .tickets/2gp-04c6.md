---
id: 2gp-04c6
status: closed
deps: [2gp-kp8p]
links: []
created: 2026-03-28T03:23:57Z
type: chore
priority: 0
assignee: user.email
parent: 2gp-c9u6
---
# Extend .env.example with commented FAL_KEY and OPENAI_API_KEY placeholders

**Normative:** **`.cursor/plans/project-implementation-deep-dive.md`** §E.0 (env names and roles).

## Design

Comment-only lines in `.env.example`; no real secrets. **`README.md`** gains (or updates) a single short instruction to copy `.env.example` → `.env` when implementing this ticket.

## Acceptance Criteria

1) `.env.example` lists **`FAL_KEY`** and **`OPENAI_API_KEY`** each with its **own** comment line: **FAL** = fal.ai spritesheet-oriented raster generation; **OpenAI** = non-fal generation + vision + chat, per §E.0. 2) **`README.md`** tells readers to copy `.env.example` → `.env` for local tooling and **names `FAL_KEY` and `OPENAI_API_KEY` at least once**.

