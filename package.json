{
  "name": "2d-game-proto",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tools/tsconfig.json",
    "generate:raster": "node --experimental-strip-types tools/fal-raster-generate.ts",
    "analyze:png": "node --experimental-strip-types tools/png-analyze.ts",
    "qa:vision": "node --experimental-strip-types tools/openai-vision-qa.ts",
    "mock:dpad-workflow": "node --experimental-strip-types tools/dpad-workflow.mjs --mode mock",
    "dpad-workflow": "node --experimental-strip-types tools/dpad-workflow.mjs",
    "generate:spritesheet": "node --experimental-strip-types tools/generate-spritesheet.mjs"
  },
  "devDependencies": {
    "@fal-ai/client": "^1.9.5",
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.5.0",
    "@types/pngjs": "^6.0.5",
    "jsdom": "^25.0.1",
    "pngjs": "^7.0.0",
    "typescript": "~5.8.0",
    "vite": "^6.2.0",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "excalibur": "^0.32"
  }
}
